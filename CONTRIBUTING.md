# 整理指引（CONTRIBUTING）

本仓库是瑞数 WAF 逆向示例集：按站点收录「能请求到数据的最小可行代码」+ 完整技术记录。
新站点、新路线、新经验按本指引整理进来，保持仓库风格与安全基线一致。

## 0. 核心原则速览

| 原则 | 一句话 |
|------|--------|
| **代码最小化** | 只留能跑通的最小代码，删调试残留；一条路线一个入口文件 |
| **README 最丰富** | 实测数据、避坑、根因、路线对比全写进 README——文档是仓库的第一资产 |
| **匿名化** | URL base64 存储运行时解码；站名用代号（site_x）+ 本地映射文件 |
| **纯算法分层** | 纯算法路线「文档开源、代码不开源」，防被直接用于大规模未授权采集 |
| **避坑即财富** | 失败路线归档（现象→根因→证据），比成功路线更有学习价值 |

## 1. 命名规范

### 目录

```
site_{代号}_{版本特征}/
  ├── README.md            # 站点总文档（必写，见 §4）
  ├── spider_{路线}.py     # 每条路线一个入口（最小化，见 §3）
  ├── pure_algo/           # 纯算法路线（仅 README 入库，见 §5）
  ├── nodenv/              # 零依赖手写补环境九件套（如需）
  ├── env/ 或 patches/     # 环境模板 / 补丁说明
  └── output/ 等运行时产物  # 一律 gitignore
```

- 代号：`site_a` ~ `site_z`，新站顺延；真实站点映射写入根 `sites_mapping.local.md`（**不入库**）
- 版本特征后缀：`base`（基础版）/ `standard`（标准版）/ `v67`（v6-v7）/ `debugger`
  （debugger 变体）/ `enhanced`（加强检测）/ `cqvip`（站点名缩写，同族站可选）
- 同一站点的多条路线放同一目录，不要一路线一目录

### 文件

- 路线入口：`spider_{sdenv|cdp|nodenv|manual_env|rs_pure|rpc|...}.py`，动词统一 `spider_`
- Node 侧：`generate_cookie.js`（sdenv）/ `run_vm.js` + `env.js`（nodenv）/ 适配器 `len{数字}.js`
- 文档：一律 `README.md`；深度攻坚记录 `{主题}详解.md` 或 `{主题}攻克思路.md`
- 命名只用 `[a-z0-9_]`，中文仅允许出现在文档文件名

## 2. 标准流程（新站点收录）

1. **验证**：至少一轮完整 412 → 200 实测，记录 cookie 长度/耗时/轮数
2. **判型**：cookie PPT 前缀 + `$_ts` 特征定版本代际（见根 README 版本分类速查）；
   与已有站点比对，同族站优先复用模板（改 URL 即过）
3. **分层整理**：
   - 纯算法路线 → 可运行代码放本地（**不入库**），技术记录写 `pure_algo/README.md`（§5）
   - 补环境/浏览器路线 → 最小化后正常入库
4. **匿名化**：URL → base64 + 运行时解码（Python `base64.b64decode` / JS
   `Buffer.from(..., 'base64')`）；站名 → 代号；删除个人绝对路径（改环境变量/自动探测）
5. **文档**：按 §4 写站点 README；更新根 README 的站点案例表、路线矩阵、决策表
6. **终检**（§6 清单）→ commit → push

## 3. 代码最小化 / 最简化原则

- **一条路线 = 一个入口文件** + 必要依赖文件，无「备用」「实验」「v1~v23」堆积
- 删掉：调试 print、注释掉的死代码、`.bak`、`*_tmp`、一次性脚本
- 依赖自动探测（环境变量 > 本目录 > 相对扫描），**不硬编码个人绝对路径**
- 参数化而非复制：同族站点用 `--site` / `--url` 参数，不复制整个脚本
- 运行时产物（cookie 文件、挑战页 HTML、ts.json/vm.js、profile）一律 gitignore
- 判断标准：别人 clone 后 `pip install` + 一条命令能复现 200，且仓库里没有一行多余的

## 4. 文档规范（README 尽可能丰富）

站点 README 至少包含：

```
# 站点代号（特征）— 标题
> 开源策略（如含纯算法路线）+ 匿名化说明（base64 解码命令）
## 站点与挑战形态       # 目标/412 特征/cookie 形态/同族判断
## 成功路线表           # 路线×文件×实测耗时×结果（表）
## 路线详解（每条一节）  # 链路图 + 关键点 + 避坑
## 已验证失败的路线     # 现象→根因→证据（重要资产，别删）
## 快速启动             # 依赖 + 一条命令
## 文件清单
```

写作要求：

- **数据说话**：实测轮数、cookie 字符数、耗时、错误码全记录（如 `5/5 轮 200，13.4-13.6s，T=250c`）
- **根因优先**：写"为什么"（根因链），不写流水账；失败路线的证据链最有价值
- **避坑列表**：每条坑 = 现象 + 根因 + 修复，能复现
- 外部引用注明来源（教程链接/博客），引用图片标注作者
- 代码片段化：文档可含关键代码片段，但**复制整合后不能运行**（省略函数体/依赖私有组件）

## 5. 纯算法路线开源策略（🔒 防滥用分层）

**背景**：纯算法方案秒级出 cookie，最容易被直接用于大规模未授权采集。为在「展示技术
实力」与「降低滥用风险」之间平衡，仓库对纯算法路线执行分层策略：

| 层 | 内容 | 是否开源 |
|----|------|---------|
| 技术记录 | 密码学结论、协议全解、字段来源、适配器结构、坑与修复、实测数据 | ✅ 开源（`README.md`/`pure_algo/README.md`） |
| 可运行代码 | 生成器、适配器、补丁、全链调度脚本 | ❌ **不开源**（本地保留 + gitignore） |
| 攻坚全程 | v1~vN 试错记录、中间产物 | ❌ 留在工作项目（如 pro42 归档），不入库 |

执行规则：

- 纯算法可运行文件统一放 `{站点}/pure_algo/`，该目录 **仅 `README.md`（和 `patches/README.md`）
  入库**，其余由根 .gitignore 的 `*/pure_algo/*` 白名单规则自动排除
- 存量站点（site_b/site_d/tax/epub）的纯算法文件已从仓库移除（本地保留），同样被 gitignore
- 站点 README / 根 README 中纯算法条目标注 🔒，技术结论照常写全
- 文档中的代码片段控制在「讲清原理即可」，缺关键函数体/依赖私有组件，拼不起来

## 6. 提交前终检清单

- [ ] URL 全部 base64；无个人路径（`C:/Users/...`）、无内部项目路径（`pro36/...`）
- [ ] 站名代号化；真实映射只进 `sites_mapping.local.md`（不入库）
- [ ] 运行时产物全部被 gitignore（`git status --ignored` 核对）
- [ ] 纯算法可运行代码未暂存（`git status` 无 `pure_algo/*.py` 等）
- [ ] 文档按 §4 齐全；根 README 三表（站点案例/路线矩阵/决策表）已同步
- [ ] 代码最小化过一遍（§3 判断标准自问）
- [ ] `py -3.12 -m py_compile` / `node --check` 语法通过
- [ ] commit message：`站点X 路线Y + 一句话亮点`（中文，同仓库风格）

## 7. 合规底线

- 本仓库全部代码仅用于 WAF 机制学习与**合规**的数据采集研究（License: MIT）
- 请遵守目标网站的服务条款与当地法律法规，勿用于任何未授权的自动化访问
- 文档中不得出现可造成实际危害的完整工具链（见 §5 分层策略）
